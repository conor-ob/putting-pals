{
  "name": "@putting-pals/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "codegen": "wrangler types",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit true",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "dev:remote": "wrangler dev --remote",
    "dev:scheduled": "wrangler dev --test-scheduled --remote",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "dependencies": {
    "@putting-pals/putting-pals-core": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "fast-json-patch": "^3.1.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.8.19",
    "@putting-pals/pga-tour-schema": "workspace:*",
    "@types/node": "catalog:",
    "drizzle-kit": "^0.31.8",
    "tsx": "^4.21.0",
    "typescript": "catalog:",
    "vitest": "~3.2.0",
    "wrangler": "^4.54.0"
  }
}
