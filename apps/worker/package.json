{
  "name": "@putting-pals/worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "codegen": "wrangler types",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:studio": "drizzle-kit studio",
    "typecheck": "tsc --noEmit true",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled --remote",
    "start": "wrangler dev"
  },
  "dependencies": {
    "@paralleldrive/cuid2": "^3.0.4",
    "@putting-pals/putting-pals-core": "workspace:*",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.1",
    "fast-json-patch": "^3.1.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@putting-pals/pga-tour-schema": "workspace:*",
    "drizzle-kit": "^0.31.8",
    "typescript": "catalog:",
    "wrangler": "^4.55.0"
  }
}
